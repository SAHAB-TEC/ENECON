<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_rgb_risk_probability_list" model="ir.ui.view">
        <field name="name">rgb.risk.probability.list</field>
        <field name="model">rgb.risk.probability</field>
        <field name="arch" type="xml">
            <list string="Probability Assessments" editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="description"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="view_rgb_risk_probability_form" model="ir.ui.view">
        <field name="name">rgb.risk.probability.form</field>
        <field name="model">rgb.risk.probability</field>
        <field name="arch" type="xml">
            <form string="Probability Assessment">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="sequence"/>
                        <field name="active"/>
                        <field name="description"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_rgb_risk_probability" model="ir.actions.act_window">
        <field name="name">Probability Assessments</field>
        <field name="res_model">rgb.risk.probability</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create probability assessment values
            </p>
        </field>
    </record>
</odoo>
