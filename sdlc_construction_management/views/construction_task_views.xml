<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree View -->
    <record id="view_construction_task_list" model="ir.ui.view">
        <field name="name">construction.task.list</field>
        <field name="model">construction.task</field>
        <field name="arch" type="xml">
            <list string="Tasks" decoration-info="state == 'draft'"
                  decoration-success="state == 'done'">
                <field name="sequence" widget="handle"/>
                <field name="reference"/>
                <field name="name"/>
                <field name="project_id"/>
                <field name="sub_project_id"/>
                <field name="assigned_to"/>
                <field name="date_start"/>
                <field name="date_deadline"/>
                <field name="priority" widget="priority"/>
                <field name="progress" widget="progressbar"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'draft'"
                       decoration-primary="state == 'in_progress'"
                       decoration-success="state == 'done'"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_construction_task_form" model="ir.ui.view">
        <field name="name">construction.task.form</field>
        <field name="model">construction.task</field>
        <field name="arch" type="xml">
            <form string="Task">
                <header>
                    <button name="action_start" string="Start" type="object"
                            class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_done" string="Done" type="object"
                            class="oe_highlight" invisible="state != 'in_progress'"/>
                    <button name="action_cancel" string="Cancel" type="object"
                            invisible="state in ('done', 'cancelled')"/>
                    <button name="action_reset_draft" string="Reset to Draft" type="object"
                            invisible="state in ('draft', 'done')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,in_progress,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="reference" readonly="1"/></h1>
                        <field name="name" placeholder="Task Name..."/>
                    </div>
                    <group>
                        <group>
                            <field name="project_id"/>
                            <field name="sub_project_id"/>
                            <field name="phase_id"/>
                            <field name="work_order_id"/>
                        </group>
                        <group>
                            <field name="assigned_to"/>
                            <field name="department_id"/>
                            <field name="priority" widget="priority"/>
                            <field name="progress" widget="progressbar"/>
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="date_start"/>
                            <field name="date_end"/>
                            <field name="date_deadline"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description"/>
                        </page>
                        <page string="Timesheet">
                            <field name="timesheet_ids">
                                <list editable="bottom">
                                    <field name="date"/>
                                    <field name="employee_id"/>
                                    <field name="description"/>
                                    <field name="hours" sum="Total Hours"/>
                                    <field name="is_internal"/>
                                </list>
                            </field>
                            <group>
                                <field name="total_hours"/>
                            </group>
                        </page>
                        <page string="Notes">
                            <field name="notes"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Kanban View -->
    <record id="view_construction_task_kanban" model="ir.ui.view">
        <field name="name">construction.task.kanban</field>
        <field name="model">construction.task</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state">
                <field name="name"/>
                <field name="project_id"/>
                <field name="assigned_to"/>
                <field name="priority"/>
                <field name="state"/>
                <field name="progress"/>
                <templates>
                    <t t-name="card">
                        <field name="priority" widget="priority"/>
                        <field name="name"/>
                        <field name="project_id"/>
                        <div>
                            <field name="assigned_to"/>
                        </div>
                        <field name="progress" widget="progressbar"/>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_construction_task_search" model="ir.ui.view">
        <field name="name">construction.task.search</field>
        <field name="model">construction.task</field>
        <field name="arch" type="xml">
            <search string="Tasks">
                <field name="name"/>
                <field name="reference"/>
                <field name="project_id"/>
                <field name="assigned_to"/>
                <filter name="draft" string="Draft" domain="[('state', '=', 'draft')]"/>
                <filter name="in_progress" string="In Progress" domain="[('state', '=', 'in_progress')]"/>
                <filter name="done" string="Done" domain="[('state', '=', 'done')]"/>
                <filter name="my_tasks" string="My Tasks" domain="[('assigned_to.user_id', '=', uid)]"/>
                <group>
                    <filter name="group_project" string="Project" context="{'group_by': 'project_id'}"/>
                    <filter name="group_assigned" string="Assigned To" context="{'group_by': 'assigned_to'}"/>
                    <filter name="group_state" string="Status" context="{'group_by': 'state'}"/>
                    <filter name="group_priority" string="Priority" context="{'group_by': 'priority'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="act_construction_project_2_construction_task_all" model="ir.actions.act_window">
        <field name="name">Tasks</field>
        <field name="res_model">construction.task</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="domain">[('project_id', '=', active_id)]</field>
        <field name="context">{
            'active_model': 'construction.project',
            'default_project_id': active_id,
        }</field>
        <field name="search_view_id" ref="view_construction_task_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new Task
            </p>
        </field>
    </record>

    <record id="construction_task_kanban_action_view" model="ir.actions.act_window.view">
        <field name="sequence" eval="10"/>
        <field name="view_mode">kanban</field>
        <field name="view_id" ref="view_construction_task_kanban"/>
        <field name="act_window_id" ref="act_construction_project_2_construction_task_all"/>
    </record>

    <record id="action_construction_task" model="ir.actions.act_window">
        <field name="name">Tasks</field>
        <field name="res_model">construction.task</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new Task
            </p>
        </field>
    </record>
</odoo>
